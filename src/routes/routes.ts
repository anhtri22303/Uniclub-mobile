enum PUBLIC {
    ROOT = 'home',
    SPLASH = 'splash',
}

enum AUTH {
    LOGIN = 'login',
    REGISTER = 'register',
    FORGOT_PASSWORD = 'forgot-password',
    RESET_PASSWORD = 'reset-password',
    OTP = 'otp',
}


export const ROUTES = {
    PUBLIC,
    AUTH,
};